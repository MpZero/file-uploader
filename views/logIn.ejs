<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="output.css" />

    <title><%= title%></title>
  </head>
  <body class="text-white font-sansation">
    <main
      class="flex flex-col bg-stone-800 min-h-dvh text-center text-sm md:text-md lg:text-3xl"
    >
      <div class="font-bold p-4 pt-8 pb-12 text-3xl lg:text-6xl">
        <h1>Log In 🔓</h1>
      </div>
      <div class="px-2 flex flex-col self-center">
        <%- include('partials/errors') %>

        <form
          class="flex flex-col gap-2 mb-8 font-mono"
          action="/log-in"
          method="POST"
        >
          <label class="w-full text-left" for="username">Username </label>
          <input
            class="text-black p-1 bg-white rounded-sm mb-1 w-full"
            type="text"
            name="username"
            id="username"
            required
          />
          <label class="w-full text-left" for="password">Password </label>
          <input
            class="text-black p-1 bg-white rounded-sm w-full"
            type="password"
            name="password"
            id="password"
            required
          />
          <%- include('partials/submitBtn',{text: "Log in"}) %>
        </form>
        <div class="flex flex-col gap-3">
          <p>Not a user? Create an account! 📝👈</p>
          <div class="flex">
            <%- include('partials/linkBtn',{text: signupBtn, link: signupLink})
            %>
          </div>
        </div>
      </div>
    </main>
  </body>
</html>
