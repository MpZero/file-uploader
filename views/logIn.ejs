<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="output.css" />

    <title><%= title%></title>
  </head>
  <body class="text-white font-sansation">
    <main
      class="flex flex-col bg-stone-800 min-h-dvh text-center text-sm md:text-md lg:text-3xl"
    >
      <div class="font-bold p-4 pt-8 pb-12 text-3xl lg:text-6xl">
        <h1>Log In 🔓</h1>
      </div>
      <div class="px-2">
        <%- include('partials/errors') %>

        <form
          class="flex flex-col gap-1.5 mb-8 font-mono"
          action="/log-in"
          method="POST"
        >
          <label
            class="self-center w-full max-w-sm lg:max-w-lg text-left"
            for="username"
            >Username
          </label>
          <input
            class="text-black p-1 bg-white rounded-sm mb-1 self-center w-full max-w-sm lg:max-w-lg"
            type="text"
            name="username"
            id="username"
            required
          />
          <label
            class="self-center w-full max-w-sm lg:max-w-lg text-left"
            for="password"
            >Password
          </label>
          <input
            class="text-black p-1 bg-white rounded-sm self-center w-full max-w-sm lg:max-w-lg"
            type="password"
            name="password"
            id="password"
            required
          />
          <button
            class="flex hover:*:-translate-y-1.5 hover:*:bg-amber-500 hover:*:border-yellow-700 self-center w-full max-w-sm lg:max-w-lg mt-2"
            type="submit"
          >
            <span
              class="grow p-2 rounded-sm border-b-8 border-yellow-600 bg-yellow-500 transition-all duration-350 ease-in-out shadow-md font-extrabold text-shadow-lg cursor-pointer"
              >Submit</span
            >
          </button>
        </form>
        <div class="flex flex-col gap-3">
          <p>Not a user? Create an account! 📝👈</p>
          <a
            class="flex hover:*:-translate-y-1.5 hover:*:bg-amber-500 hover:*:border-yellow-700 self-center w-full max-w-sm lg:max-w-lg"
            href="/sign-up"
            ><span
              class="grow p-2 rounded-sm border-b-8 border-yellow-600 bg-yellow-500 transition-all duration-350 ease-in-out shadow-md font-extrabold text-shadow-lg"
              >Sign up</span
            ></a
          >
        </div>
      </div>
    </main>
  </body>
</html>
