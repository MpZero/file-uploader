<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="output.css" />

    <title><%= title %></title>
  </head>
  <body class="text-white font-sansation">
    <main
      class="bg-stone-800 min-h-dvh text-center xs:text-xs md:text-md lg:text-lg xl:text-2xl"
    >
      <div class="px-2 lg:grid lg:grid-cols-4">
        <div
          class="font-bold py-4 text-sm md:text-md lg:text-xl col-span-full xl:text-2xl"
        >
          <div class="flex gap-1 pb-2 text-left border-b border-stone-700">
            <p>Welcome to</p>
            <p>File Uploader 📂,</p>
            <p><%= username %></p>
          </div>
        </div>
        <div
          class="flex flex-col font-mono lg:col-start-1 lg:px-1 lg:border-r lg:border-stone-600"
        >
          <form
            class="flex flex-col gap-1 md:grid grid-cols-2 lg:flex lg:gap-4 lg:px-0.5 lg:pt-0"
            action="/folders"
            method="post"
          >
            <label
              class="bg-stone-700 rounded-sm p-1 self-center w-full md:col-span-2"
              for="folderName"
              >Add a folder
            </label>
            <input
              class="p-1 text-black bg-white rounded-sm mb-1 self-center md:p-3 md:m-0 md:row-start-2 w-full"
              type="text"
              placeholder="Folder name"
              name="folderName"
              id="folderName"
              required
            />
            <%- include('partials/submitBtn',{text: "Create folder"}) %>
          </form>
          <div class="grow text-center"><%- include('partials/errors') %></div>
          <div
            class="hidden font-mono lg:flex flex-col gap-1 items-start pr-0.5 pt-8 md:text-md xl:text-xl"
          >
            <%- include('partials/navbar') %>
          </div>
        </div>
        <div
          class="mt-2 text-left font-roboto-condensed lg:col-span-3 lg:mt-0 lg:pl-1"
        >
          <div class="flex mb-1 gap-1">
            <p
              class="bg-stone-700 hover:bg-stone-600 p-1 h-full cursor-pointer rounded-sm w-full md:p-2"
            >
              Name 🔽
            </p>
            <p
              class="bg-stone-700 hover:bg-stone-600 p-1 h-full cursor-pointer rounded-sm w-full md:p-2 text-right"
            >
              View ⊞
            </p>
          </div>
          <ul class="flex flex-col">
            <% if (folders) { %> <%folders.map((folder) => { %>
            <li class="flex gap-2 hover:*:bg-stone-700">
              <a
                class="w-full p-2 pl-1 rounded-sm transition-all duration-250 linear text-shadow-lg truncate"
                href="/folders/<%=folder.id %>"
                >📁 <%= folder.name %></a
              >
            </li>
            <% }); %> <% }; %>
          </ul>
        </div>
      </div>
    </main>
  </body>
</html>
