<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="output.css" />
    <title><%= title%></title>
  </head>
  <body class="text-white font-sansation">
    <main
      class="flex flex-col bg-stone-800 min-h-dvh text-center text-sm md:text-md lg:text-xl"
    >
      <div class="font-bold p-4 pt-6 pb-10 text-3xl lg:text-6xl">
        <h1>Sign Up 📝</h1>
      </div>
      <div class="flex flex-col gap-3 px-2 min-w-xs self-center">
        <%- include('partials/errors') %>
        <form
          class="w-full flex flex-col items-center gap-1.5 mb-8 font-mono"
          action="/sign-up"
          method="POST"
        >
          <label class="w-full max-w-sm lg:max-w-lg text-left" for="username"
            >Username:
          </label>
          <input
            class="text-black p-1 bg-white rounded-sm mb-1 w-full max-w-sm lg:max-w-lg"
            type="text"
            name="username"
            id="username"
            required
          />
          <label class="w-full max-w-sm lg:max-w-lg text-left" for="password"
            >Password:
          </label>
          <input
            class="text-black p-1 bg-white rounded-sm mb-1 w-full max-w-sm lg:max-w-lg"
            type="password"
            name="password"
            id="password"
            required
          />
          <label
            class="w-full max-w-sm lg:max-w-lg text-left"
            for="confirmPassword"
            >Confirm Password:
          </label>
          <input
            class="text-black p-1 bg-white rounded-sm mb-1 w-full max-w-sm lg:max-w-lg"
            type="password"
            name="confirmPassword"
            id="confirmPassword"
            required
          />
          <%- include('partials/submitBtn',{text: "Sign up"}) %>
        </form>
        <div class="flex flex-col gap-3">
          <p>Already a user? Log in!👇</p>
          <div class="flex">
            <%- include('partials/linkBtn',{text: loginBtn, link: loginLink}) %>
          </div>
        </div>
      </div>
    </main>
  </body>
</html>
