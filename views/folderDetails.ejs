<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/output.css">
  <title><%= title %></title>
</head>
<body class="text-white font-sansation">
  <main
  class="flex flex-col gap-1 px-1 bg-stone-800 min-h-dvh text-center text-xs  md:text-xl lg:grid grid-cols-4 lg:auto-rows-[auto_1fr] lg:text-2xl">
    <div class="flex justify-between border-b p-1 border-stone-400  col-span-full font-inter ">
      <div class="flex gap-1 items-center">
        <a class="p-1 rounded-sm cursor-pointer hover:bg-stone-700" href="/folders">⬅ </a>
        <h1 class="font-bold p-1"><%= folder.name %></h1>
        <p class="p-1 font-bold"> <%= folder.createdAt.toLocaleString() %>
        </p>
      </div>
      <div class="flex gap-1 items-center">
        <p class="p-1 rounded-sm cursor-pointer hover:bg-stone-700">🔎</p>
          <div class="flex"><%- include('partials/burgerBtn') %></div>

        
      </div>
    </div>
    <div class=" hidden font-mono lg:flex flex-col gap-1 items-start  p-1 border-r border-gray-400 text-2xl xl:text-xl lg:col-end-2">
      <%- include('partials/navbar') %>
    </div>
    <div class="lg:col-start-2 col-span-full">
      <div class="flex py-1 gap-1 text-xs md:text-lg lg:col-start-2 col-span-full lg:text-xl">
        <%- include('partials/linkBtn',{text:"Upload file", link:`/folders/${folder.id}/upload`}) %>
        <%- include('partials/linkBtn',{text:"Update folder", link:`/folders/${folder.id}/update`}) %>
        <%- include('partials/linkBtn',{text:"Delete folder", link:`/folders/${folder.id}/delete`}) %>
      </div>
    <div class="flex gap-1 text-xs lg:text-xl col-start-2 col-span-full">
      <p
      class="bg-stone-700 hover:bg-stone-600 p-1 cursor-pointer  rounded-sm w-full text-left"
      >
      Name 🔽
      </p>
      <p
      class="bg-stone-700 hover:bg-stone-600 p-1  cursor-podiver rounded-sm w-full  text-right"
      >
      View ⊞
      </p>
    </div>
    <%- include('partials/errors') %>
      <div class="lg:col-start-2 col-span-full p-1">
        <% if(files.length !== 0) { %>
          <ul class=" text-xs lg:text-lg flex flex-col gap-1 font-roboto-condensed">
            <% files.map((data) => { %>
              <div class="flex   justify-between  border-b-1 border-stone-400 py-1 hover:bg-stone-700 transition-all duration 350 ease-in-out ">
                <div class="grid grid-cols-2 text-left pl-1 ">
                  <li class="col-span-2"><p><%= data.filename %></p></li>
                  <li class="col-start-1" ><p><%=data.size%> kb </p></li>
                  <li class="col-start-2" ><p><%=data.createdAt.toDateString() %></p></li>
                </div>
                <div class="flex gap-1 ">
                  <li class=" flex hover:*:-translate-y-0.5 hover:*:bg-amber-400 hover:*:border-yellow-600  "><a class=" p-2 rounded border-b-4 text-shadow-md shadow-md               border-amber-600  cursor-pointer   bg-amber-500
                  transition-all duration-350 ease-in-out
                  md:p-4 lg:p-6" href="/folders/<%=folder.id%>/file/<%= data.id%>/update"> ✏️</a></li>
                  <li class=" flex hover:*:-translate-y-0.5 hover:*:bg-green-400 hover:*:border-green-600 "><a class="p-2 rounded border-b-4 text-shadow-md border-green-600  cursor-pointer   bg-green-500
                  transition-all duration-350 ease-in-out
                  md:p-4 lg:p-6" href="/folders/<%=folder.id%>/file/<%= data.id%>/download">⤵️</a></li>
                  <li class=" flex  hover:*:-translate-y-0.5 hover:*:bg-red-400 hover:*:border-red-600 "><a class="p-2 rounded border-b-4 text-shadow-md border-red-600  cursor-pointer   bg-red-500 transition-all duration-350 ease-in-out md:p-4 lg:p-6" href="/folders/<%=folder.id%>/file/<%= data.id%>/delete">🗑️</a></li>
                </div>
              </div>
              <%}) %>
            </ul>
          <%} %>
        </ul>
      </div>
    </div>
  </body>
</html>
